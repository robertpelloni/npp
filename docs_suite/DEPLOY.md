# Notepad++ Deployment Instructions

*(This document is currently a placeholder as the build system is transitioning from a Win32/Visual Studio focus to a cross-platform CMake/Go focus. It will be updated as the CI/CD pipeline evolves.)*

## Current Build (C++)
Use `CMake` to build the current C++ version.

```bash
mkdir build
cd build
cmake ..
make
```

## Future Build (Go)
To be documented once the Go port initialization is complete.
